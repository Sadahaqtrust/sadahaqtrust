"use client";
import EditLayout from "@/app/components/EditLayout";

export default function SadahaqEditWrapper({ children }: { children: React.ReactNode }) {
  return <EditLayout>{children}</EditLayout>;
}
